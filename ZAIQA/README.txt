How to run: Open index.html in your browser.
Required assets: Ensure the 'images' folder contains the referenced PNG files.

Changes made:
- Fixed accessibility (ARIA, focus states, semantic HTML).
- Improved performance (Lazy loading, preconnect).
- Enhanced SEO (Meta tags, semantic structure).
- Fixed mobile navigation (Accessibility, event handling).
- Scaled up design as requested previously.

Verification Checklist:
1. Open DevTools (F12) > Console. Ensure no red errors.
2. Use Tab key to navigate. Ensure focus ring is visible on all links/buttons.
3. Resize browser to mobile width. Check hamburger menu works.
4. Run Lighthouse audit (in DevTools). Expect high scores.
